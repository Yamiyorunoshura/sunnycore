# Copy to .env and set values to enable running CI-like local tests

# Provider name and model for promptfoo, e.g.:
#   openai:gpt-4o
#   anthropic:claude-3-5-sonnet-latest
#   vertex:gemini-1.5-pro
PROMPTFOO_PROVIDER="anthropic:claude-3-5-sonnet-latest"

# Optional: override provider base URL (useful for OpenAI-compatible gateways)
# Examples:
#   https://api.openai.com/v1
#   https://api.openrouter.ai/v1
#   https://your.custom.gateway/v1
PROMPTFOO_BASE_URL=""

# Optional: unified API key for the selected provider.
# If set, the script will map it to the correct env var
# (e.g., OPENAI_API_KEY or ANTHROPIC_API_KEY) when empty.
PROMPTFOO_API_KEY=""

# Common provider keys (set those you use)
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GOOGLE_VERTEX_PROJECT=""
GOOGLE_APPLICATION_CREDENTIALS=""

# Optional: proxy or region settings
HTTP_PROXY=""
HTTPS_PROXY=""
